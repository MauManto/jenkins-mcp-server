# =============================================================================
# Jenkins Instance Configuration
# =============================================================================
# You can configure multiple Jenkins instances using either format:
#
# 1. Default instance (backward compatible):
JENKINS_URL=https://your-jenkins-url.com
JENKINS_USER=your_username
JENKINS_API_TOKEN=your_api_token

# 2. Named instances (for multiple Jenkins servers):
#    Pattern: JENKINS_<INSTANCE_NAME>_URL, JENKINS_<INSTANCE_NAME>_USER, JENKINS_<INSTANCE_NAME>_API_TOKEN
#
# Example - Legacy Jenkins:
# JENKINS_LEGACY_URL=https://jenkins-legacy.example.com
# JENKINS_LEGACY_USER=legacy_user
# JENKINS_LEGACY_API_TOKEN=legacy_token
#
# Example - Current/Production Jenkins:
# JENKINS_CURRENT_URL=https://jenkins.production.example.com
# JENKINS_CURRENT_USER=current_user
# JENKINS_CURRENT_API_TOKEN=current_token

# =============================================================================
# Usage with Multiple Instances
# =============================================================================
# When calling the tools, you can specify which instance to use:
#
# 1. Full URL: "https://jenkins-legacy.example.com/job/MyJob/lastBuild"
#    (automatically detects the matching instance)
#
# 2. Instance prefix: "legacy:MyFolder/my-job"
#    (matches against JENKINS_LEGACY_* or URLs containing "legacy")
#
# 3. Simple job name: "MyFolder/my-job"
#    (uses default JENKINS_URL or first available instance)

# SSL Verification (set to false for self-signed certificates)
JENKINS_VERIFY_SSL=true

# Optional Configuration
MAX_LOG_SIZE=250000
CONTEXT_WINDOW=2

# HTTP Timeout Configuration (in seconds)
HTTP_TIMEOUT=30
HTTP_CONNECT_TIMEOUT=10
HTTP_READ_TIMEOUT=120
HTTP_WRITE_TIMEOUT=10

# Server Configuration
SERVER_PORT=3000
SERVER_PATH=/mcp

# Debug mode - set to true for verbose logging
DEBUG=false
